from sklearn.compose import ColumnTransformer
from pipeline.build_pipeline import num_pipeline
from pipeline.build_pipeline import cat_pipeline


num_transformer = ColumnTransformer = (transformers=[
    ()
])
